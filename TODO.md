# PROCESS

Work proceeds in delegated chunks (lettered below), each sized for one agent session and ending in a reviewable state: a branch with a focused diff, plus a short memo appended to `notes/LOG.md` stating what was done, what changed in outputs, what was verified, and open questions. Nothing merges to `main` without Colin reading the memo and diff; each merged chunk moves to DONE with the date and commit hash, so DONE doubles as an audit trail. Check-ins are weekly (~30 min): review the latest chunk, resolve open questions, and assign the next one. Two standing rules keep the project legible after time away: (1) no hard-coded numbers — every figure cited in `paper.Rmd` or `slides.tex` comes from a scalar CSV in `output/results/` written by an estimation script, and the full chain `derived/ -> program/ -> output/ -> paper.pdf` must rebuild via `make`; (2) a single spec sheet, `notes/specs.md`, records the exact estimating equation, sample restrictions, fixed effects, and clustering for every table and figure in the paper, and is updated in the same commit as any spec change. The paper text is checked against the spec sheet, not against memory.

Verification is built in rather than ad hoc. `program/tests/` holds fake-data tests run by `make test`: each estimator is applied to simulated data with known parameters (e.g., claims data with a built-in MH x post-1994 damage effect, common vintage effects, and geography-period shocks) and must recover the truth; the same harness demonstrates *why* the spec is right (e.g., that omitting common vintage effects biases the MH-vintage coefficients). Constructed denominators get the same treatment: any imputation must pass an exact adding-up test against its source totals and a fake-data test that recovers a known take-up gradient. Data-construction steps carry the standard sanity checks (uniqueness on intended IDs, cross-field consistency, missing-value accounting, counts validated against external benchmarks like Census 2000 MH counts and MHS shipments). Agent sessions run `make test` before opening a review; any red test blocks the merge. Review comments in `coarse-output/*_review.md` are the external checklist — each addressed comment gets flipped to `[Addressed]` in that file in the commit that addresses it.

# TODO

Timeline anchors: JMP-heavy through August (Chunk A only, low effort); September for B–D, with C1 early in that block since D–G all inherit its numbers; late September to mid-October for E–F; welfare table (G) once C and E land; slides and dry run by late October for APPAM (early November).

## Chunk C — Cost side: wind-zone dose-response [agent; Colin reviews interpretation]
Addresses review target 4 and comment 7. The current binary treatment (any Zone II/III county in the state) is badly diluted: pooled across treated states only **30%** of the 1980–2000 MH stock sits in Zone II/III, ranging from FL 97% and LA 64% down to GA 6% and VA 3%. Taken literally the binary $5,000 implies something near $17,000 in a fully-treated market, which strains credulity — so this test is diagnostic either way.
- [X] Build state-level continuous treatment intensity: MH-stock-weighted share of counties in Zone II/III, from `derived/ecfr-windzone.csv` × `derived/census2000-mh-county-vintage.Rds`. Write to `derived/` and export the state table as a paper appendix. Done: `derived/mhs-windzone-intensity.Rds`, `output/descriptives/windzone-intensity.tex`, appendix Table `tab:windzone-intensity` in `paper.Rmd`.
- [X] Re-estimate the MHS price DiD (Eq. 1) with continuous intensity replacing the binary `treated`; also report the binary version restricted to high-intensity states only (FL, LA, MA). Done: `est_p_dose`, `est_p_hi` in `estimate-mhs.R`; plus a static binary-vs-continuous comparison table (`tab:mhs-dose-response`, Table 2 in `paper.Rmd`, same N/FE/clustering across both columns).
- [X] Interpret and record: a flat gradient supports regional production standardization (whole states effectively treated, binary design vindicated, $5,000 stands); a steep gradient means the true per-unit compliance cost is substantially larger and the benefit-cost ratio worsens. Report whichever obtains; do not condition the framing on the sign. **Result: the gradient is steep.** Event-study-average implied fully-treated effect ≈ $8,116 vs. binary's $4,194 (ratio 1.94); static single-coefficient version ≈ $4,108 vs. $2,775 (ratio 1.48). Both cuts agree on direction: true compliance cost > binary $5,000 headline. See `notes/specs.md` §11.
- [ ] **Open: decide the paper's headline compliance-cost figure.** Currently still $5,000-ish from the binary spec (`price_effect_level`) throughout the abstract/intro/discussion, and feeding straight into Chunk G's welfare table (BCR, NPV). Candidates: keep binary as the conservative headline and report dose-response as robustness; switch to the static continuous estimate (~$4,108, closest in spirit to `post_mh`'s single-coefficient convention); or the event-study-average continuous estimate (~$8,116). Whichever is chosen, the swap touches the abstract, intro, discussion, and conclusion (`price_eff`, `price_pct` in `paper.Rmd`'s setup chunk) and should be decided before Chunk G runs, since it changes the BCR. Colin to decide at next check-in.
- [ ] Correct the institutional text: § 3280.305 sits in Part 3280, the Construction and Safety *Standards*, so the 1994 wind rule is a manufacturing/design requirement indexed to intended siting location (data plate states the zone), not an installation standard. Installation standards (Part 3285) came later — verify the date against the source before asserting it. Checked 2026-08-12: current `paper.Rmd` institutional-background text already describes the rule correctly (construction standard, not installation), so no text change was needed; the Part 3285/date claim itself was not independently verified against the eCFR source.
- [ ] Benefit-side companion: dose-response on damages using `treated_wz3`, already carried in `nfip-balanced.Rds`. MH sited in Zone III should improve more than Zone I if the wind channel is real. Not started — out of scope for the cost-side pass (confined to `databuild-mhs.R`/`estimate-mhs.R`).

## Chunk D — Mechanism decomposition [agent]
- [X] Re-run main spec (static `post_mh`, `building_damage`) with/without elevated buildings; by SFHA status; by Chunk C's Zone III wind-zone crosswalk (`derived/ecfr-windzone.csv`, coordinated rather than an independent coastal-county list). `est_mech_split` in `estimate-nfip.R`, exported to `output/event-study/countyfp/mechanism-splits.tex` (not yet wired into `paper.Rmd`). **Result: effect concentrated in non-elevated buildings** (−5.96*, elevated ≈ 0, n.s.) — per comment 15, elevation only rises post-1998 so this isn't a composition artifact, but Colin's read (2026-08-13) is that it cuts against the tie-down/wind-standard mechanism (elevated homes should be more wind-exposed, so a real wind-standard effect would be expected to show up there, not in the flood-exposed non-elevated group). SFHA split is a wash (−3.29 non-SFHA vs. −3.34 SFHA, both weak). Zone III point estimate is larger than Zone I-II (−6.57 vs. −3.83) but not significant (SE 3.95) — directionally consistent with Chunk C, not dispositive on its own.
- [ ] **Open: decide if/how to present the heterogeneity in claim-level damages by policy characteristics** (elevated/SFHA/wind-zone splits above, `est_mech_split` in `estimate-nfip.R`). Colin is punting for now — the elevation result muddies rather than supports the proposed mechanism, and none of the three splits is individually convincing. Candidates: omit from the paper entirely; report as an honest complication in a robustness/mechanism section; or revisit with more power (interaction spec instead of sample split) before deciding.
- [X] Insurance-accounting outcomes: capped-payment indicator, payment-to-coverage ratio, damage-to-replacement-cost ratio, probability of zero/small payment (addresses "payments ≠ physical loss"). Done but **superseded** (Colin, 2026-08-13): second-order to the paper's actual question (physical damage to MH), so moved to a `# Superseded` section at the end of `estimate-nfip.R`, console-only output, no `.tex` export. Results for the record: capped-payment and payment/coverage both fall slightly post-1994 (−0.027*, −0.051***) — argues against, not for, "coverage caps mute the measured improvement"; damage-to-replacement-cost ratio and zero/small-payment probability both null.

## Chunk E — Housing-stock denominator and the take-up margin [agent; all inputs already on disk]

**Decision (2026-08-11): build this from public data, not CoreLogic.** CoreLogic assessment data does carry construction vintage, so a within-county vintage hedonic on 2023 assessed values is a legitimate design and is *not* ruled out — it is deferred on cost. What argues against it for the denominator specifically: chattel-titled MH are personal property and frequently absent from real-property assessment rolls, so its MH universe skews land-titled (the wrong subpopulation for this paper), and a 2023 snapshot conditions on surviving the very floods that generate the outcome.

**The outcome being fixed.** `estimate-nfip.R` currently computes `policies_ppermit = policies_n / permits_sf_n` (lines 170–172, 182–183, 351–357; dict entry line 74; already partly disabled). This is the wrong take-up margin for three reasons: the denominator is *single-family permits* and is applied to MH and site-built rows alike; BPS covers only permit-issuing jurisdictions, undercounting badly and non-randomly (see the diagnostic in DONE); and `databuild-nfip.R:306` spreads county permits uniformly across tracts, an arbitrary allocation. Replace it with `policies_per_home = policies_n / homes_n`, where `homes_n` is the stock of that housing type and vintage in that county.

- [X] **New script `program/import/impute-stock.R`.** Output `derived/stock-county-vintage.Rds`, keyed `countyfp × year_constr × mh`, with `homes_n`, covering construction years 1984–1999. Done: levels from Census 2000, within-bin allocation from MHS state-year placements (MH) and BPS county-year permits with state fallback (site-built); 1980-1983 and 1994 dropped per the bullets below. No `$DATA_PATH` needed — verified standalone.
- [~] **Staged delivery — do the free version first.** Not done as a separate 2×2 Census-only landing; went straight to the full imputed version since the imputation itself was cheap and fully verifiable without `$DATA_PATH`. The free 1980s-vs-1995-98 Census-only contrast is a subset of what `homes_n` now supports, not built as its own standalone table — flag if a Census-only robustness column is still wanted separately.
- [X] **Extend `program/import/databuild-nfip.R`** to merge `stock-county-vintage.Rds` by `(countyfp, year_constr, mh)`. Done as specified: county-level column, ratio formed after aggregation in estimation; `permits_sf_n` and the section-5.5 per-tract split retired. Verified by patching a copy of the existing `nfip-balanced.Rds` (no `$DATA_PATH` in this session); the real run is still open, see below.
- [X] **Rework the take-up estimation** in `estimate-nfip.R`. Done: `policies_ppermit` and its dict entry dropped; `policies_per_1k_homes`/`claims_per_1k_homes` added; `take-up.tex` estimates OLS on those ratios directly (`homes_n`-weighted, clustered by county — swapped from an initial PPML-with-offset version at Colin's request 2026-08-13; unweighted OLS was tried and rejected, see `notes/LOG.md`), decomposed into policies-per-1,000-homes and claims-per-1,000-homes columns (claim frequency vs. the existing damage tables' payment-conditional-on-claim). Table trimmed to 2 columns (pooled sample only) after checking that an earliest-period-only (2009-2013) restriction added no information over the pooled estimate. Result reads as no clear extensive-margin take-up shift, not the earlier PPML version's +7%/+22%.
- [X] **Rewrite the take-up appendix** (`paper.Rmd`). Done — see review target 2.
- [X] **Validation.** All done inside `impute-stock.R`: adding-up, uniqueness, non-negativity, stability (0.86-0.95 correlation across Census-bin pairs), national benchmark (ratio 0.95 vs. cumulative MHS shipments). Fake-data test added: `program/tests/test-take-up-imputation.R`.
- [~] **Caveats to write into the paper, not just the code.** Time-gap caveat and the 2009-2013-only restricted column both written/built. **ACS bounding itself not implemented** — no `import-acs.R` exists in this repo and building one was out of this chunk's data-already-on-disk scope; noted as an open item in `notes/LOG.md` and as a caveat (not a bound) in the paper text.

## Chunk F — Composition-controlled specs and closer comparisons [agent]
- [ ] Specifications controlling flexibly for replacement cost, coverage, elevation, SFHA, mandatory-purchase status (review target 1; extend the existing `+ Controls` robustness block and put it in the paper). **From Chunk C1's review:** these variables are mediators, not confounders — Table `tab:composition` shows them moving after 1994, and adding them to the main spec moves the static building-damage estimate from −3.68 to roughly −3.0 to −3.1 (Chunk C1 diagnostics), i.e. the mediated share of the effect, not bias removed. Label the `+ Controls` columns (`fmla_rob_b`, `fmla_rob_d` in `estimate-nfip.R`) as a decomposition rather than a cleaner estimate; the bounding argument in "Selection and Composition" is the stronger response to the composition concern on its own.
- [ ] Alternative comparison groups: MH-only pre/post-1994 within wind zones II/III vs zone I (state x vintage variation); tighten the conservative-bias language per comment 2 (capped-payment analysis from Chunk D feeds this).

## Chunk G — Welfare table and literature calibration [agent drafts; Colin signs off on assumptions]
Depends on C (cost may move) and E (claim rate gets a real denominator), so run it after both.
- [ ] Replace the back-of-envelope with a disciplined cost-benefit table (review target 5): rows = assumptions (claim rate variants, discount rate 3/5/7%, lifespan 20/30yr); columns = private / fiscal (NFIP + FEMA IA) / social returns; include uncertainty intervals from estimation SEs and a break-even calculation for the omitted wind channel. All cells from scalars.
- [ ] Label the $5,000 as price incidence, not engineering compliance cost, throughout — and update it if Chunk C's dose-response moves the number.
- [ ] Replace the insured-claim claim rate with the population claim rate from Chunk E's denominator; report both and say which the welfare table uses.
- [ ] Calibration table placing estimates beside Dehring & Halek, Simmons et al., Simmons & Sutter, Baylis & Boomhower in common units (upfront cost, annual expected loss reduction, PV benefit-cost ratio, hazard channel).

## Chunk H — APPAM deliverables [Colin leads; agent assists]
- [ ] Refresh `slides.tex`: new welfare table, take-up results, wind-zone dose-response, mechanism splits; 12–15 min student-panel format; frame via ROAD to Housing Act topicality.
- [ ] Update draft with Chunks B–G merged; recompile; circulate to advisor.
- [ ] Dry run with timing before travel.

## Chunk I — Vintage window, annualized take-up, and p(claim | policy) [agent]
Branch `chunk-i-window-claim-rate`. Colin's request 2026-08-26: move the effect window to 1984–1999, add p(claim | policy) as a third take-up column, and start on the composition-vs-technology question.
- [X] **Window 1984–1999.** `MIN_YEAR_CONSTR` 1983 → 1984 in `project-params.R` (with the reasoning in the comment); `databuild-nfip.R` now reads both bounds from there instead of restating literals, so the panel grid and the estimation-time restriction cannot drift. Every two-year bin now holds two full construction years and the window coincides exactly with the range over which `homes_n` is defined. Panel and claims rebuilt from research-database. Event-study coefficients per outcome: 7 (was 8); `paper.Rmd` said "eleven" in two places, corrected.
- [X] **Annualized the per-home outcomes.** `policies_per_1k_homes`/`claims_per_1k_homes` were five-year cumulative counts labelled as rates (`policies_n` counts policy-*years* summed over a five-year `period_loss` bin). Renamed with a `_yr` suffix and divided by `N_YEARS_PERIOD = 5L`, which is asserted against the data. `paper.Rmd`'s panel description also said each cell "reports the number of unique policies active at any time during the period" — corrected to policy-years, with the midpoint-year assignment stated.
- [X] **Third margin: claims per policy-year.** `claim_rate` was estimated and plotted but never tabled. Now column (3) of `tab:take-up` and the new `tab:take-up-static`, on the same cells as columns (1)–(2) so `claims/home = policies/home × claims/policy` holds. **Result: flat.** Static +0.64 claims per 1,000 policy-years (SE 0.65) against a pre-1994 MH rate of 14.7, no significant vintage bin. This is the hazard rate the cost-benefit rests on, so the whole of the estimated benefit comes from severity, not frequency.
- [X] **Fixed IID standard errors on the four cell-level fits.** `est_pclaim_es`, `est_comp_post`, `est_share_es`, `est_pois_es` passed no `cluster` argument, so `fixest` reported IID SEs — while both `paper.Rmd`'s table notes and `notes/specs.md` §3 claimed clustering. Same bug Chunk C1 fixed on the claim-level side. Now `cluster = ~geo`. SEs widen materially; several post-1994 composition coefficients are no longer significant, which forced the rewrite below.
- [X] **Rewrote "Selection and Composition."** The old text asserted post-1994 MH policies have higher replacement cost ($20–30k), building coverage ($8–15k), *and* contents coverage ($1–2k), and read the damage estimates as a lower bound on that basis. The table does not show that. What it shows: large pre-1994 coverage gaps (MH building coverage much lower, contents slightly higher for mid-1980s vintages) that shrink to roughly a quarter of their size by 1990–1991, and small, mostly insignificant post-1994 coverage shifts. The one clean post-1994 shift is SFHA share, rising monotonically. The section now says that, keeps the direction-of-bias argument but rests it on SFHA rather than coverage, and drops the "lower bound" phrasing from the intro. The table notes also described `Primary res.` and `Mandatory` columns that are commented out of the spec.
- [X] ~~**Documented the take-up column (1) artifact.**~~ Superseded by Chunk I-b below: the −4.9 coefficient, the site-built jump at 1994/1995, and the NFIRA explanation were all artifacts of two denominator defects. The four level scalars stay; the NFIRA text is gone from the paper.
- [X] **Closed: NFIRA 1994 tested and ruled out.** Split column (1)'s numerator into mandatory-purchase and non-mandatory policy-years on the same denominator, weights, FE and clustering, so the two coefficients sum to the total: mandated +0.79 (0.20), non-mandated +7.33 (2.56). Nine-tenths of the movement is in policies the homeowner was not required to buy, so a non-mandatory-only version of the specification would give the same answer and is not needed. Caveat: the flag marks only 4–9% of policy-years, far below the SFHA share, so it under-records mandate exposure and the split is a lower bound on the mandated part.
- [ ] **Open: `estimate-welfare.R` uses event-study-average deltas while the paper's headline is the static estimate.** Pre-existing, unchanged by this chunk, but now visible: the abstract quotes $4.11k for building damage (static) while the cost-benefit applies $4.88k (event-study average). Contents runs the other way ($2.77k event-study vs $4.07k static), so switching to static deltas would *raise* the BCR from ~0.52 to ~0.56 rather than lower it. Same class of decision as Chunk C's open compliance-cost question and should be settled with it.

## Chunk I-b — Two take-up denominator defects (2026-08-26)
Found while answering Colin's mandatory-purchase question. Full detail in `notes/specs.md` §12.2.
- [X] **Fixed: within-bin shares were normalized over retained years only** (`impute-stock.R`). The Census vintage bins are wider than the 1984–1999 window, so dropping 1980–1983, 1994 and 2000 from `bin_years` reallocated their stock onto the years that remained — pre-1994 denominators inflated ~1.5× (1984–89) and ~1.3× (1990–93) while the fully retained 1995–1998 bin was correct, i.e. bias along the treatment split. New `bin_span` table normalizes over every year each bin spans (2000 at 3/12, since the last bin is "1999 to March 2000"), then subsets. The old adding-up test asserted the defect; it is now an exact share-level invariant plus a bound at the allocation step. National imputed MH stock benchmark moves from ratio 0.95 to 0.75, as expected.
- [X] **Fixed: the 1994 vintage bin mixed a two-year numerator with a one-year denominator** (`estimate-nfip.R`). `dt_home_cell` was a row filter on `dt_cell`, whose `policies_n`/`claims_n` were already summed over construction years 1994 and 1995, while `homes_n` exists only for 1995. Construction year 1994 supplies ~48% of that bin's policy-years, so its measured rate was inflated ~1.9× — at exactly the treatment boundary. `dt_home_cell` is now rebuilt from the row level with numerator and denominator restricted to the same `(county, construction year, type)` cells, asserted by `stopifnot`. The unsafe per-home columns were removed from `dt_cell`.
- [X] **Result reverses sign.** Static take-up −4.88 (3.06) → **+8.12 (2.71)**; claims per 1,000 homes 0.018 (0.040) → **+0.110 (0.055)**; claims per policy unmoved at 0.0005 (0.0007), still flat. N 73,487 → 71,492. Damage results and `estimate-welfare.R` untouched — neither uses `homes_n`.
- [X] **Rewrote the take-up results text.** Since claims per policy is flat, the identity assigns nearly all of the claims-per-home rise to take-up, which is the opposite of the crowding-out prediction. Reported as descriptive rather than causal, for three stated reasons: the vintage profile trends rather than steps (earliest pre-reform bin −14.2, SE 3.05); the sign depends on the geographic fixed effects (the same contrast without them is −2.05, SE 1.49, since MH stock concentrates in counties whose overall take-up rose least); and the denominator is imputed while the numerator is not.
- [ ] **Open: the trending pre-period in column (1) is unexplained.** A monotone pre-1994 profile in a take-up rate is consistent with survivorship in the Census-2000 denominator (older vintages lose more stock, and differentially by housing type) rather than with anything about the 1994 standard. Bounding it needs a vintage-specific attrition estimate — the same ACS continuous-vintage series already listed as an open item under Chunk E's caveat. Until then column (1) stays descriptive.

## Chunk J — Policy-level composition [agent]
Colin's decision 2026-08-26: replace the cell-level composition table with a policy-level one, and **do not keep the cell-level version in the paper.**
- [ ] `program/import/databuild-nfip-policy.R` → `derived/nfip-policy-micro.parquet`, one row per policy term (1984–1999 construction, 2009–2023 midpoint years, deflated to 2000 dollars).
- [ ] Composition spec `i(period_constr, mh, ref = 1992) | countyfp^period_loss + mh + period_constr`, clustered by county, restricted to single-family residential — verify the `occupancy_type` code list against the FEMA data dictionary first, since there are two coding eras and MH rows use both 1 and 14. The single-family restriction is motivated by the diagnostic behind Chunk I's rewrite: the 1980s coverage gaps come largely from a tail of multi-family and commercial policies in the site-built comparison group, not from MH selection.
- [ ] Add the contents-coverage choice margin: `1[contents coverage > 0]` and contents coverage given positive. Roughly 30–35% of MH policies carry no contents coverage, and that share rises with vintage for MH while falling for site-built — the most interesting risk-economics object in the table.
- [ ] Remove `tab:composition`'s cell-level input and the cell-level composition fit from `estimate-nfip.R` once the replacement lands.

## Chunk K — Composition vs. technology (folds into Chunk F) [agent]
Colin's choice 2026-08-26 of the three simpler tests; **reweighting (entropy balance / DFL) is punted** — "see if the simpler fixes (single-family restriction, non-parametric controls for water depth) help out" first.
- [ ] Non-parametric water-depth controls and depth-bin × MH interactions, plus a damage-function figure. Handle the post-1994 rise in MH water-depth missingness to 16%.
- [ ] Single-family-only comparison group applied to the claim-level damage specs as well as the policy specs.
- [ ] Winsorize or trim the damage-share denominator and report it beside the baseline. `building_value` has extreme outliers (99.9th percentile $3.9M for MH, $1.02B for site-built), which is the weakest link in the `building_damage_share` outcome.

## Deferred (post-APPAM / job-market permitting)
- [ ] CoreLogic vintage hedonic on 2023 assessed values (county × vintage, site-built as comparison) as an independent read on the capitalized value of the standard. Revisit if Chunk C suggests the $5,000 needs scrutiny; weigh against licensing cost and the chattel-titling coverage gap.
- [ ] Florida ~2005 site-built code reform as symmetric evidence on flood damages and take-up (carried over).
- [ ] Worked disaster case: one major event (Harvey/Ian) merging claims to flood-depth grids; damage-depth curves by type x vintage.
- [ ] Direct wind-channel evidence (2004 FL hurricane season; IA claims) or break-even benchmark against published wind-loss estimates.
- [ ] Affordability incidence: $5,000 under typical chattel-loan terms; HMDA (post-2004 only, no vintage) for ownership margins.
- [ ] Power/robustness ideas surfaced during Chunk C1 but not scoped there: 32% of claims have zero net building payment and the level distribution is severely right-skewed — `est_claim_pois` is already estimated (`estimate-nfip.R:265`) but never reported; PPML and IHS give much weaker proportional effects than OLS levels (1999 cohort: OLS −5.88 (2.17); IHS −0.216 (0.108); PPML −0.077 (0.094)), worth stating explicitly rather than letting a referee find it. Also: permutation inference on the 1994 cutoff (with effectively one treated unit, an exact p-value from reassigning the threshold across vintages is cheap and persuasive), and `HonestDiD` sensitivity in place of resting on "flat pre-trends" (pre-period SEs of ~1.9 against effects of 4–6 mean the pre-test has limited power to detect violations that would matter).

# DONE
- [X] Evaluate whether MH indicator is present in NFIP during relevant period; otherwise, consider imputation. Checked 'locationOfContents' and 'numberOfFloorsInTheInsuredBuilding' fields for MH detection.
- [X] (2026-08-11) Confirmed the main claim spec already includes common vintage FE (`period_constr` in `estimate-nfip.R:243`) — review comment 1 is a paper-notation fix, not a code bug. Found paper-vs-code geography discrepancy (statefp vs county/tract labels) — resolution assigned to Chunk A.
- [X] (2026-08-11) Measured BPS permit coverage against Census 2000 site-built units for the 1995–1998 vintage, by quartile of county MH share. Median ratio falls monotonically: 0.70 (low-MH) → 0.69 → 0.40 → 0.20 (high-MH), with 68% of high-MH counties under half. Permits are not completions so some shortfall is expected everywhere, but the gradient is the permit universe — rural counties without permit requirements are exactly where MH concentrate. Conclusion: BPS cannot serve as a level denominator; usable only for within-bin timing shares. This is what disqualifies `policies_ppermit`.
- [X] (2026-08-11) Quantified dilution in the MHS cost design. Of 3,109 counties only ~175 are Zone II/III, all inside 11 states holding ~1,000 counties. MH-stock-weighted Zone II/III share by state: FL 97%, LA 64%, MA 46%, SC 25%, MS 17%, NC 14%, ME 11%, AL 9%, TX 9%, GA 6%, VA 3%; pooled 30%. Motivates the Chunk C dose-response.
- [X] (2026-08-11) Decided against CoreLogic for the stock denominator; see Chunk E header for reasoning and the deferred list for the one CoreLogic design still worth revisiting.
- [X] (2026-08-12) **Chunk B — review quick fixes.** Text/notation/units batch (comments 1, 3-5, 7-15, 20-23) plus panel-structure clarifications (16-18); 21 of 23 detailed comments now `[Addressed]` in the review file (comment 2 belongs to Chunk F, comment 6 was already done). Notation: `\lambda_{\nu_i}` and `k \neq 1992` in Eq. (2), `k \neq 1993` in Eq. (1). Damage shares restated in percentage points using the static estimate (the event-study share coefficients run -22.4 to +0.8, so the old "between X% and Y%" range was both mislabeled and misleading). Table 1 damage rows relabeled `$000s`. `period_loss` renamed "Calendar period" throughout the cell-level tables. Conclusion and abstract now state the flood channel recovers ~21% of the price increase rather than "a substantial share." Detail in `notes/LOG.md`.
- [X] (2026-08-11, `997c8eb`) **Chunk A — verification harness, spec sheet, and stale-output reconciliation.** `program/tests/` fake-data harness plus `make test` (NFIP claims ES, MHS price DiD, welfare arithmetic); `make` stage targets chaining `derived/ -> program/ -> output/ -> paper.pdf`, verified end-to-end from a clean `paper.pdf`; `notes/specs.md` written covering every table/figure. Resolved the geography discrepancy in favor of **county x loss-period** as the baseline (specs.md §5) — the main claim spec had been hardcoded to `statefp^year_loss` and reading stale `output/event-study/statefp/` output, so headline numbers moved. Also fixed the missing `period_constr` FE in Tables 3-4. Open items carried forward: `make data` (raw import layer) still unverified on a machine with `$DATA_PATH` set; stale `output/event-study/statefp/` left in place.
- [X] (2026-08-12, `6a45acd`) **Chunk C1 — baseline-spec fixes for the NFIP claim design.** Clustered all claim-level specs by county (`cluster = ~countyfp`); set `BIN_CONSTR_YEAR` default to `2L` (paper already claimed two-year bins); extended `MIN_YEAR_CONSTR` to `1983L` for a longer pre-period; added the static `post_mh` TWFE as the headline number (Table `tab:claims-outcomes-static`), replacing the noisy 11-coefficient event-study average. Headline outcome confirmed as `building_damage` (Colin, 2026-08-12) — fixed the `fig:es-building-damage` caption/outcome mismatch to match. Full detail in `notes/specs.md` §10 and `notes/LOG.md`. Open question carried forward: whether to extend `MAX_YEAR_CONSTR` past 1999 (check the 2005 construction-year bin for a possible sentinel value first).
